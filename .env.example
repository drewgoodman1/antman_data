### MinIO credentials and ports
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_PORT=9100
MINIO_CONSOLE_PORT=9101
MINIO_BUCKET=antman-lake

### S3 endpoint and auth for DuckDB httpfs
# For local MinIO. For DuckDB httpfs, prefer host:port without scheme in SQL.
# In Python env vars, both can work, but our configs reference this.
S3_ENDPOINT_URL=localhost:9100
S3_ACCESS_KEY_ID=${MINIO_ROOT_USER}
S3_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
S3_REGION=us-east-1
S3_USE_SSL=false

